exports = (typeof window === 'undefined') ? global : window;

exports.countAnswers =  {
  count : function (start, end) {
  	

  // 	while(start<=end)
  // 	setTimeout(function(){
  // 		console.log(start++);
  // 	}, 100)
   }
}
